version: 1

graph: examples/regulator/circuit-design.dot
task: "implement the design described in examples/regulator/docs/48V_design_brief.md"

repo:
  path: /Users/dpeckham/code/orgs/qhcorp/kilroy

cxdb:
  binary_addr: 127.0.0.1:9009
  http_base_url: http://127.0.0.1:9010
  autostart:
    enabled: false

llm:
  cli_profile: real
  providers:
    anthropic:
      backend: api
      api:
        protocol: anthropic_messages
        api_key_env: ANTHROPIC_API_KEY
        base_url: https://api.anthropic.com
        path: /v1/messages
        profile_family: anthropic

modeldb:
  openrouter_model_info_path: /Users/dpeckham/code/orgs/qhcorp/kilroy/internal/attractor/modeldb/pinned/openrouter_models.json
  openrouter_model_info_update_policy: on_run_start
  openrouter_model_info_url: https://openrouter.ai/api/v1/models
  openrouter_model_info_fetch_timeout_ms: 5000

git:
  require_clean: false
  run_branch_prefix: attractor/run
  commit_per_node: true

inputs:
  materialize:
    enabled: true
    imports:
      - pattern: "examples/regulator/docs/**/*"
        required: true
      - pattern: "docs/**/*.md"
        required: false
    fan_in:
      promote_run_scoped: []
    follow_references: false
    infer_with_llm: false

artifact_policy:
  profiles: ["generic"]
  env:
    managed_roots:
      tool_cache_root: "managed"
    overrides:
      generic: {}
  checkpoint:
    exclude_globs: []

runtime_policy:
  stage_timeout_ms: 0
  stall_timeout_ms: 600000
  stall_check_interval_ms: 5000
  max_llm_retries: 6

preflight:
  prompt_probes:
    enabled: true
    transports: [complete]
    timeout_ms: 15000
    retries: 1
    base_delay_ms: 500
    max_delay_ms: 5000
